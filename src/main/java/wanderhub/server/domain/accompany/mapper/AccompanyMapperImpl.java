package wanderhub.server.domain.accompany.mapper;

/*
@Component
public class AccompanyMapperImpl implements AccompanyMapper {
    @Override
    public AccompanyDto toDto(Accompany accompany) {
        if (accompany == null) {
            return null;
        }
        AccompanyDto dto = AccompanyDto.builder()
                .id(accompany.getId())
                .memberId(accompany.getMemberId())
                .displayName(accompany.getDisplayName())
                .local(accompany.getLocal())
                .maxNum(accompany.getMaxNum())
                .title(accompany.getTitle())
                .content(accompany.getContent())
                .status(accompany.isStatus())
                .createDate(accompany.getCreateDate())
                .modifyDate(accompany.getModifyDate())
                .build();
        return dto;
    }

    @Override
    public Accompany toEntity(AccompanyDto dto) {
        return Accompany.builder()
                .id(dto.getId())
                .memberId(dto.getMemberId())
                .displayName(dto.getDisplayName())
                .local(dto.getLocal())
                .maxNum(dto.getMaxNum())
                .title(dto.getTitle())
                .content(dto.getContent())
                .status(dto.isStatus())
                .createDate(dto.getCreateDate())
                .modifyDate(dto.getModifyDate())
                .build();
    }
}*/
